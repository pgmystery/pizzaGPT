[project]
name = "pizzagpt"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.12.5",
    "langchain>=1.0.1",
    "sqlmodel>=0.0.27",
]

[tool.uv.workspace]
members = [
    "apps/pizzagpt-client",
    "apps/pizzagpt-mcp",
]
